[pytest]
# Make both the repo root (so `import agent` resolves) and mcp-server/ (so
# `from app.structured_logger import ...` resolves) available without requiring
# the caller to set PYTHONPATH manually.
# Requires pytest >= 7.0 (we use 9.0.2).
pythonpath = . mcp-server
